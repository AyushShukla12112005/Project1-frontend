services:
  - type: web
    name: signature-app-frontend
    runtime: node
    buildCommand: npm install && node node_modules/vite/bin/vite.js build
    startCommand: node node_modules/serve/build/main.js dist -s -p $PORT
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
